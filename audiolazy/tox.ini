[tox]
envlist = py27, py32, py33, py34, pypy
setupdir = ..
toxworkdir = ../.tox

[testenv]
deps = pytest
       pytest-cov
       pytest-timeout
       numpy
       scipy
       sympy
commands = py.test

[testenv:pypy]
deps = pytest
       pytest-cov
       pytest-timeout
       sympy

[pytest]
addopts = --doctest-modules
          --cov-config tox.ini
          --cov-report term-missing
          --cov audiolazy

[run]
branch = True
omit = test/*
